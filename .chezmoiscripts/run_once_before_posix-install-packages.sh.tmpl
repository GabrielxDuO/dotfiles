{{- if has .chezmoi.os (list "darwin" "linux" "freebsd" "openbsd" "netbsd") -}}
#!/usr/bin/env bash
set -e

if ! command -v vp >/dev/null 2>&1; then
  curl -fsSL https://vite.plus | VP_NODE_MANAGER=no bash
fi
{{ end -}}
